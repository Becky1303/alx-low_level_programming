#include "main.h"

/**
* add - adds two integers and returns the result
*
* Description: adds two integers
*
* Return: Sum of two integers
*/

int add(int a,  int b)
{
	int tot_sum;
	tot_sum = (a + b);
	return (tot_sum);
}
